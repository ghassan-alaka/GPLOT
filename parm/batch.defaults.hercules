# Default batch information for MSI Hercules
CPU_ACCT = aoml-hafs1
QOS = batch
PARTITION = hercules
sbatch = /opt/slurm/bin/sbatch
squeue = /opt/slurm/bin/squeue
# Offline cartopy Natural Earth cache (exported as CARTOPY_DATA_DIR by batch_*.sh)
cartopy_dir = /home/role-aoml-hafs1/.local/share/cartopy
